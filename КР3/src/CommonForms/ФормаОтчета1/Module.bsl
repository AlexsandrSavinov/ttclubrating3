
&НаСервере
Процедура ПередЗагрузкойПользовательскихНастроекНаСервере(Настройки)
	
	мНастройкиПоОтбору = Ложь;
	//по идее настройка всегда будет, но были ошибки не понятно почему.
	Параметры.Свойство("НастройкиПоОтбору", мНастройкиПоОтбору);
	
	Если мНастройкиПоОтбору Тогда
		Для Каждого ТекОтбор Из Настройки.Элементы Цикл
			Если ТипЗнч(ТекОтбор) = Тип("ЭлементОтбораКомпоновкиДанных") Тогда
				ТекОтбор.Использование = Ложь;
			КонецЕсли; 
		КонецЦикла; 
	КонецЕсли; 
	
КонецПроцедуры
