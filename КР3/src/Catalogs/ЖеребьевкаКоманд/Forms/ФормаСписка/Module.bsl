
&НаКлиенте
Процедура фПерезаполнитьШаблоны(Команда)
	
	ПерезаполнитьНаСервере();	
	ПоказатьОповещениеПользователя("Заполнение шаблонов",,"Перезаполнение шаблонов жеребьевки закончено!",БиблиотекаКартинок.Информация);
		
КонецПроцедуры

&НаСервере
Процедура ПерезаполнитьНаСервере()
	Справочники.ЖеребьевкаКоманд.ЗаполнитьШаблоныЖеребьевок();
	Элементы.Список.Обновить();
КонецПроцедуры
 