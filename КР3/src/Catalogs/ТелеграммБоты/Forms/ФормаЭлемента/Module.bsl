
&НаКлиенте
Процедура Проверить(Команда)
	Если ЗначениеЗаполнено(Объект.Токен) Тогда
		РаботаССоциальнымиСетямиКлиентСервер.ПроверитьДоступностьТелеграмм(Объект.Токен);
	Иначе
		Сообщение = Новый СообщениеПользователю;
		сообщение.ИдентификаторНазначения = УникальныйИдентификатор;
		Сообщение.Текст = "Не заполнен токен бота.";
		Сообщение.Поле = "Объект.Токен";
		Сообщение.Сообщить(); 
	КонецЕсли; 
КонецПроцедуры
